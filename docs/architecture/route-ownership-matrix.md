# Route Ownership Matrix

This matrix records what each public route owns, what shared systems it consumes, and what future branches must avoid changing without explicit scope.

This is an audit artifact, not an implementation branch. The generic web development research aligns with Vox Mana's current direction through semantic HTML, OKLCH tokens, container queries, reduced motion, native platform patterns, and route-level polish. These should remain selective enhancements, not a repo-wide modernization mandate.

## Audit Scope

- Reviewed public route HTML, route CSS, route JS entrypoints, shared CSS/JS, frontend validators, smoke tests, manual QA docs, project atlas, and generic webdev research.
- Public routes covered: `index.html`, `guide/index.html`, `guide/maze/index.html`, `archscry/index.html`, `maze/index.html`, `strategium/index.html`, `apocrypha/index.html`, `library/index.html`, `privacy/index.html`, and `terms/index.html`.
- VM-143 created this matrix as an audit artifact without changing runtime code. VM-413 later updated typography delivery and route heads while leaving generated data, Supabase schema, placement logic, precon ranking, and route behavior untouched.

## Shared Systems

| System | Files | Consumed By | Ownership Notes |
|---|---|---|---|
| Shared tokens and layer base | `assets/css/tokens.css`, `assets/css/fonts.css`, `assets/css/layout.css` | All public routes except route-specific alias details | Site-wide visual foundation. Treat as high-blast-radius CSS. |
| Shared atmosphere | `assets/css/atmosphere.css`, `assets/js/shared/vm-rich-atmosphere.js` | Archscry, Maze, Apocrypha, Privacy, Terms | Canvas/animated background behavior is visually sensitive and partly masked by harnesses. Home owns its route-local atmosphere inside `home.js`. |
| Shared topbar | `assets/css/topbar.css`, `assets/js/shared/vm-topbar.js` | All canonical public topbars, including the Library compatibility shell | Owns the five-destination primary pill, Guide/Feedback/menu utility hierarchy, local-file-safe targets, mobile projection, active-state geometry, and reduce-motion toggle bridge. |
| Shared components | `assets/css/components.css` | Archscry, Maze, Apocrypha, Library, Privacy, Terms | Reusable panels/buttons/shell styles. Do not assume Home or Strategium consumes every component rule. |
| Reduce motion state | `assets/js/shared/reduce-motion.js`, `assets/js/shared/vm-topbar.js` | All main public routes except Library alias | Persists `vm_reduce_motion` in localStorage and mirrors state onto document attributes. |
| Local reading persistence | `assets/js/shared/shared.js` | Archscry, Maze | Owns placement-result normalization, verified `vm_archscry_saved_reading_v1` persistence, exact-key legacy migration, Forget cleanup, and the in-memory current-reading bridge. |
| Chart runtime | `assets/js/vendor/chart.umd.js` | Archscry | Large vendored Chart.js runtime used by the active Archscry radar. It is local, not CDN-loaded. |

## Public Route Matrix

| Route | Page Purpose | Entry HTML | CSS Stack | JS Entrypoints | Data Dependencies | Browser Storage | External Services | Generated Files Used | Smoke / Manual Tests | Known Risks | Do Not Touch Without Scope |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `/` | Canonical home gateway. | `index.html` | `tokens.css`, `fonts.css`, `layout.css`, `topbar.css`, `home.css` | `home.js`, `reduce-motion.js`, `vm-topbar.js` | Static Home markup and route-local atmosphere. | `vm_reduce_motion` via shared reduce-motion/topbar. | None route-owned. | None. | `npm run lint:html`, `npm run test:frontend-smoke`; visual comparison remains Owner-directed. | Home atmosphere is animation-sensitive. | Home route targets, stars/orbs, pointer variables, reduced-motion/visibility behavior, back-to-top, local-file route compatibility, and accepted layout. |
| `/guide/` | Concise Field Guide front door for first decisions and whole-product orientation. | `guide/index.html` | `tokens.css`, `fonts.css`, `layout.css`, `topbar.css`, `atmosphere.css`, `components.css`, `guide.css` | `reduce-motion.js`, `vm-feedback.js`, `vm-topbar.js` | Static route copy only. | `vm_reduce_motion` via shared reduce-motion/topbar. | None route-owned. | None. | `npm run lint:html`, `npm run test:route-metadata`, `npm run test:frontend-smoke`, `npm run test:topbar-browser`, focused desktop/mobile/keyboard/reduced-motion review. | Product-map hierarchy can accidentally imply a mandatory linear flow; Guide must remain one utility entry on desktop/mobile while the five-link primary pill stays balanced. | Whole-product orientation only; no specialist semantics, product state, or invented deep links. |
| `/guide/maze/` | Optional depth for reading Maze translation, retained context, Commander-color relation, recovery, and result actions. | `guide/maze/index.html` | `tokens.css`, `fonts.css`, `layout.css`, `topbar.css`, `atmosphere.css`, `components.css`, `guide.css`, `guide-maze.css` | `vm-rich-atmosphere.js`, `reduce-motion.js`, `vm-feedback.js`, `vm-topbar.js` | Static specimens pinned to current deterministic Maze outputs. | `vm_reduce_motion` via shared reduce-motion/topbar. | None route-owned. | None. | `npm run test:maze-onboarding`, `npm run test:maze-onboarding-browser`, `npm run test:guide-browser`, route metadata/smoke/lint checks, focused desktop/mobile/deep-link/reduced-motion/zoom review. | Static specimens can drift from the compiler or imply that a retained reading changes a query; pinned fixtures and exact context wording protect those boundaries. | Parser/query semantics, persistence, Reading Finds schema/drawer, automatic broadening, full Scryfall reference content, and the three-mode introduction already owned by the working Maze and `/guide/`. |
| `/archscry/` | Primary quick adaptive reading and saved-result dossier, plus the public read-only Identity Atlas at `?explore=atlas` and identity dossiers at `?explore=<slug>`. | `archscry/index.html` | `tokens.css`, `fonts.css`, `layout.css`, `topbar.css`, `atmosphere.css`, `components.css`, `archscry.css` | `shared.js`, `graph.js`, `vm-radar.js`, `index.js` module, `identity-directory.js`, `identity-atlas.js`, `dossier-view.js`, `reduce-motion.js`, `vm-rich-atmosphere.js`, `vm-topbar.js` | `data/factions.json`, `data/gate-b1-placement-model.json`, `data/identity-layers.json`, dossier/taxonomy/precon catalogs, optional Scryfall indexes. Atlas membership, grouping, labels, display color order, and dossier destinations are projected from active registry/faction records rather than copied records. | `vm_archscry_saved_reading_v1` is the sole persistent saved-reading authority. Exact retired profile/legacy/pending keys are compatibility migration inputs only. Maze handoff, Reading Finds, media caches, and reduced motion retain separate purposes. Atlas/exploration adds no persistence key. | Scryfall API and web links, EDHREC, MTGDecks, Archidekt, Moxfield, Google fallback search. | Existing faction, identity-layer, dossier, precon, taxonomy, and Scryfall artifacts; no Atlas-specific identity database. | `npm run test:retired-runtime`, `npm run test:identity-atlas`, `npm run test:dev-review`, `npm run test:reading-guide`, `npm run lint:html`, `npm run lint:js`, focused frontend/browser smoke and owner review. | Explicit `explore` routing must outrank passive saved restoration; browse copy must not imply assessment; Guild/College entries must remain distinct; Atlas/history/Maze journeys must not mutate a saved reading. | Placement scoring/qualification, generated identity content, registry authority, precon ranking/schema, saved-result shape, persistent Maze handoff, Reading Finds semantics, and the accepted identity-card visual direction. |
| `/maze/` | The Implicit Maze Scryfall search console with Plain Reading, Operator's Hand, The Loom, results, modal, Reading Finds, and Archscry reading or transient browsed-dossier context. | `maze/index.html` | `tokens.css`, `fonts.css`, `layout.css`, `topbar.css`, `atmosphere.css`, `components.css`, `maze.css` | `shared.js`, `maze-handoff.js`, `research-init.js` module, `vm-rich-atmosphere.js`, `reduce-motion.js`, `vm-topbar.js` | `data/maze/scryfall-parser-seed-2026.json`, Scryfall API responses, normal Archscry handoff payload, device-local v1 saved reading, or explicit query-carried `identity-explore` context. | Existing Maze handoff, Reading Finds, cache, dictionary, and reduce-motion keys retain their distinct purposes. `identity-explore` is transient and may not write a placement, Reading Finds association, or persistent Archscry handoff. A forgotten handoff cannot restore placement data. | Scryfall API/web, TCGPlayer product search. | None directly; consumes Scryfall seed and runtime API data rather than generated site JSON. | `npm run test:retired-runtime`, `npm run test:identity-atlas`, `node tests/maze/maze-search-tests.js`, existing Maze onboarding/Finds/frontend-smoke tests, and focused owner return-copy review. | Explicit browsed identity must win over stale saved handoff while active, then return to that dossier without changing the separately saved reading. Normal saved-reading Maze behavior and VM-624 Loom remain protected. | Parser/search behavior, modal semantics/inert behavior, normal Archscry return/handoff, mode IDs/actions, approved Maze visual treatment, Reading Finds local-first/no-account boundary, persistence schema, and query semantics. |
| `/strategium/` | Commander learning console for table expectations, archetype library, threat reading, readiness, and identity pressure. | `strategium/index.html` | `tokens.css`, `fonts.css`, `layout.css`, `topbar.css`, `strategium.css` | `strategium.js`, `reduce-motion.js`, `vm-topbar.js` | Route-local static data inside `strategium.js`. | `vm_reduce_motion` via shared reduce-motion/topbar; checklist state is runtime-local unless future code adds persistence. | None route-owned. | None directly. | `npm run lint:html`, `npm run lint:js`, `npm run test:visual:strategium`, manual Strategium targeted portal and checklist passes. | Visual polish is closely tied to VM-133 and VM-142 comparisons; canvas/atmosphere behavior is local to the route. | Tab IDs/actions, checklist state behavior, archetype search semantics, approved glass/readability treatment. |
| `/apocrypha/` | Canonical public reference archive and provenance page. | `apocrypha/index.html` | `tokens.css`, `fonts.css`, `layout.css`, `topbar.css`, `atmosphere.css`, `components.css`, `apocrypha.css?v=20260521b` | `reduce-motion.js`, `vm-topbar.js`, `apocrypha.js?v=20260521b` | Static page content and public reference links. | `vm_reduce_motion` via shared reduce-motion/topbar. | Outbound public reference links. | None directly. | `npm run lint:html`, `npm run test:visual:apocrypha`, `npm run test:frontend-smoke` for Library alias, manual Apocrypha visual pass. | `/library/` depends on this route as canonical destination; versioned asset query strings are part of cache behavior. | `/apocrypha/` canonical route, `/library/` alias compatibility, public-only reference framing, no private-source leakage. |
| `/library/` | Legacy compatibility alias that forwards to Apocrypha. | `library/index.html` | Shared CSS stack, small inline alias shell CSS | Inline redirect script plus meta refresh/noscript fallback. | None. | None route-owned. | None route-owned. | None. | `npm run lint:html`, `npm run test:frontend-smoke`, manual alias check. | Intentional inline style/script exception; root-relative URLs are guarded by validator. | Relative-safe redirect to `../apocrypha/`, alias messaging, compatibility behavior. |
| `/privacy/` | Privacy policy with Vox Mana-specific service glossary. | `privacy/index.html` | `tokens.css`, `fonts.css`, `layout.css`, `topbar.css`, `atmosphere.css`, `components.css`, `legal.css` | `reduce-motion.js`, `vm-topbar.js` | Static legal copy. | `vm_reduce_motion` via shared reduce-motion/topbar. | Mentions Google, Supabase, Anthropic, and Scryfall in policy copy; no route-owned service calls. | None. | `npm run lint:html`, `npm run test:frontend-smoke`, shared topbar/legal manual pass, responsive legal visual QA. | Legal text mentions service names that can confuse simple grep-based runtime scans. The page intentionally shares the gateway visual family through the legal background shell and `legal.css`; hero opacity follows the VM-142 primary `0.72 / 0.56` glass, while summary/sections follow the lighter `0.62 / 0.44` glass without blur. | Legal copy, service disclosure wording, gateway background treatment, VM-142-matched legal opacity, `legal.css` last-load contract, shared nav route targets. |
| `/terms/` | Terms of service with Vox Mana-specific user and service framing. | `terms/index.html` | `tokens.css`, `fonts.css`, `layout.css`, `topbar.css`, `atmosphere.css`, `components.css`, `legal.css` | `reduce-motion.js`, `vm-topbar.js` | Static legal copy. | `vm_reduce_motion` via shared reduce-motion/topbar. | Mentions third-party services in legal copy; no route-owned service calls. | None. | `npm run lint:html`, `npm run test:frontend-smoke`, shared topbar/legal manual pass, responsive legal visual QA. | Legal copy must stay consistent with actual services. The page intentionally shares the gateway visual family through the legal background shell and `legal.css`; hero opacity follows the VM-142 primary `0.72 / 0.56` glass, while summary/sections follow the lighter `0.62 / 0.44` glass without blur. | Legal copy, service limitation wording, gateway background treatment, VM-142-matched legal opacity, `legal.css` last-load contract, shared nav route targets. |

## Follow-Up Card Targets

- Route-contract documentation is delivered by VM-143 and this matrix.
- VM-144 archived the proven-dead preview-era assets from active `assets/` paths into `docs/research/archive/vm144-stale-preview-assets/`. The canonical Home route is `index.html`; the live Home assets are `home.css` and `home.js`, and any remaining legacy preview-era names are historical only.
- VM-145 completed legal page CSS extraction, VM-152 realigned the legal pages to the current public-route visual family, and VM-153 matched legal glass opacity to the VM-142 Maze/Strategium decision; future legal-page styling work should stay presentation-only, keep `legal.css` scoped to Privacy/Terms, and preserve policy text.
- VM-146 completed the CDN/font dependency review in `docs/architecture/cdn-font-dependency-review.md`; VM-413 closed the deferred Google Fonts delivery work for live public routes. Future dependency work should keep external asset delivery separate from product/service behavior and policy-copy mentions.
- Large CSS/JS reduction should be route-by-route with visual harnesses, not shared-system churn.

## Current Verification Baseline

- `npm run lint:html` validates public script deferral, image sizing, landmarks, topbar navigation semantics, Maze modal semantics, route-local CSS extraction, Library alias safety, and legal nav targets.
- `npm run lint:js` syntax-checks the main frontend route scripts and guards against inline handler patterns/body-level HTML injection.
- `npm run test:frontend-smoke` verifies critical home, Maze, Archscry, Library alias, Privacy, and Terms route contracts.
- `npm test` covers placement, parser, builder, syntax translation, Maze metadata, precon artifacts, Archscry dossier follow-ups, and presentation snapshots.
- `npm run dossier:audit` currently reports warnings but no failures; treat warnings as known dossier quality work, not a route ownership blocker.
