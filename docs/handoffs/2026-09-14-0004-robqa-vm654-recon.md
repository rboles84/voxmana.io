# Independent RobQA handoff — VM-654 retired-code recon

Date: 2026-09-14

Task: VM-654
Candidate: dacca18131ccbb7b8c807a0fb388df1d68598426
RobQA: PASS
Execution: SEPARATE
Reviewer: /root/recon_qa
Implementer: Codex coordinator plus RobDev workers

## Agent and configuration

- Agent name: `/root/recon_qa`, independent RobQA reviewer.
- Task requested: inspect the exact zero-production-change recon candidate, choose proportionate evidence, and prepare Owner Review without implementing the proposed purge or replacing Owner judgment.
- Governing authority: repository-local `.agents/skills/robqa/SKILL.md`, full `docs/qa/RobQAPass.md`, the VM-654 card, and the repository workflow.
- Requested and accepted route: role `robqa`, model `gpt-5.6-sol`, effort `medium`; backend model identity, billing, effective remote reasoning effort, and token savings were not exposed and remain unverified.

## Change classification

- QA tier: QA-0, documentation/recon evidence only.
- Changed behavior: none. The candidate records current-source findings and a proposed future retirement sequence.
- Protected behavior intentionally untouched: deterministic quick reading; local v1 saved-reading persistence, restoration, Forget and Begin Again; Atlas/dossier; Maze context, return and Reading Finds; placement/model; cards/Scryfall; radar; Home atmosphere and back-to-top.
- QA execution mode, reviewer/agent, and risk-based reason: SEPARATE by `/root/recon_qa`. Although the candidate is QA-0 by changed bytes, its assertions govern shared state, security, account retirement, migration ordering, generated-context dependencies, and future protected integration.
- Exact candidate SHA and evidence reference: `dacca18131ccbb7b8c807a0fb388df1d68598426`; evidence packet `docs/audits/retired-code-recon/README.md` and linked reports.
- Candidate scope: Git reports 22 baseline-to-candidate paths, all confined to the admitted recon records, handoffs, VM-654 card, and generated workflow views. Production, tests, source/generated data, package/workflow definitions, deployment sources, and earlier task lifecycle records have zero changed paths.

## Files reviewed

- Exact diff `01e11dc3e6cdae683e9f8120a0034f26d33806f1..dacca18131ccbb7b8c807a0fb388df1d68598426` and correction diff `c6617bb7ea3b69f92eba26b3761825a28caa5375..dacca18131ccbb7b8c807a0fb388df1d68598426`.
- VM-654 card, recon README, runtime manifest, shared-storage, Archscry/Maze, Home/routes, Supabase/deck, generated-context provenance/dependencies, tests/CI, documentation inventory, both TSV inventories, raw reference hits, probe source/result, and all four implementation/specialist handoffs.
- Targeted unchanged source for every named shared API and `VM_SESSION` consumer; Archscry state/reset/boot/action/DOM paths; Maze storage/context paths; Home load order; deck-link imports/gates/renderers; faction-context producer/readers; package/CI; and current Privacy, Terms, and root README claims.

## Material candidate

- Baseline: `01e11dc3e6cdae683e9f8120a0034f26d33806f1`
- Candidate: `dacca18131ccbb7b8c807a0fb388df1d68598426`
- Changed paths: `22`

## Files changed

- `docs/audits/retired-code-recon/README.md`
- `docs/audits/retired-code-recon/archscry.md`
- `docs/audits/retired-code-recon/documentation-additional-paths.tsv`
- `docs/audits/retired-code-recon/documentation-inventory.md`
- `docs/audits/retired-code-recon/documentation-paths.tsv`
- `docs/audits/retired-code-recon/faction-context-provenance.md`
- `docs/audits/retired-code-recon/home-routes.md`
- `docs/audits/retired-code-recon/reference-hits.txt`
- `docs/audits/retired-code-recon/runtime-manifest.md`
- `docs/audits/retired-code-recon/shared-probes.json`
- `docs/audits/retired-code-recon/shared-probes.mjs`
- `docs/audits/retired-code-recon/shared-storage.md`
- `docs/audits/retired-code-recon/supabase-current-dependencies.md`
- `docs/audits/retired-code-recon/supabase-deck.md`
- `docs/audits/retired-code-recon/tests-ci.md`
- `docs/handoffs/2026-09-14-0000-codex-vm654-retired-code-recon.md`
- `docs/handoffs/2026-09-14-0001-robdev-vm654-home-routes.md`
- `docs/handoffs/2026-09-14-0002-robdev-vm654-supabase-deck.md`
- `docs/handoffs/2026-09-14-0003-robdev-vm654-archscry.md`
- `docs/handoffs/HANDOFF_INDEX.md`
- `docs/kanban/board.md`
- `docs/kanban/in-progress/VM-654-retired-code-recon.md`

This exact list and count come from Git across the stated baseline and candidate. Review-time `HEAD` equals the candidate and was clean before this QA evidence was authored. This handoff is the reviewer's sole evidence-only write outside the material candidate; the coordinator owns its card/index binding and final-branch accounting. It does not change the candidate, proposed purge, production, lifecycle state, Git index, or Owner decision.

## Tests selected

- Test: `git diff --check 01e11dc3e6cdae683e9f8120a0034f26d33806f1 dacca18131ccbb7b8c807a0fb388df1d68598426`. Reason: mandatory QA-0 formatting check across the exact candidate. Result: PASS.
- Test: exact Git path accounting and excluded-scope query. Reason: prove the zero-production-change contract. Result: PASS; 22 admitted documentation/workflow paths and zero paths outside recon/handoff/Kanban scope.
- Test: `npm run task -- indexes --check` before this QA handoff was added. Reason: verify candidate generated views. Result: PASS, 697 cards and 1094 handoffs. Adding this handoff requires the coordinator to regenerate/check both views as evidence-only follow-up.
- Test: TSV schema/count/path/set verification. Reason: the audit claims exhaustive baseline document-path inventories without reading 967 historical bodies. Result: PASS. Primary TSV has columns `path|category|disposition|basis`, 840 unique existing paths, and exact 840/840 regenerated-query parity. Supplemental TSV has the same schema, 127 unique existing paths: exact 125/125 regenerated-query parity plus the two declared instruction fences.
- Test: `node docs/audits/retired-code-recon/shared-probes.mjs`. Reason: cheaply verify source-level local-storage, reset, Forget, legacy precedence and failed-persistence claims without browser or service state. Result: PASS, 8 assertions in isolated memory storage.
- Test: read-only generated-context projection using current `buildFactionContext`, model/display JSON, and parsed context module. Reason: verify the proposed migration prerequisite. Result: PASS: 37 committed keys, 37 projected keys, identical order, zero differing entries, and exact metadata after the producer's intentional `gate_compression` omission.
- Test: targeted exact-name and call-site source tracing. Reason: test the material truth of the recon. Result: PASS. It confirms named API consumers; complete shared-state/storage ownership; reset/retake/Forget ordering and residual inputs; Home's eager `VMRadar` dereference plus registry/Chart work before the absent-canvas guard; no live pure deck provider consumer outside the retired family; active generic dossier links; current generated-context tool/test consumers; test/CI dispositions; and the current legal-copy boundary.

One mistaken command, `npm run docs:check-indexes`, returned “Missing script.” It was not a repository check and supplied no candidate evidence. The canonical `npm run task -- indexes --check` command above passed.

## Tests intentionally skipped

- Suite: product browser, visual regression, screenshot, and viewport runs. Why not required: no runtime, DOM, CSS, route, interaction, or visual bytes changed; there is no changed objective browser behavior to prove. Last valid baseline/certification: unchanged baseline `01e11dc3e6cdae683e9f8120a0034f26d33806f1`; this recon neither renews nor invalidates its product certification.
- Suite: placement journeys, all-37 UI replay, semantic mutation/recovery, and broad regression. Why not required: placement/model/data/producer behavior did not change; the read-only 37-entry comparison directly checks the recon assertion. Last valid baseline/certification: unchanged admitted baseline.
- Suite: live Supabase/RLS/OAuth, remote deployment, browser-profile storage, and cloud-data inspection. Why not required: VM-654 is repository-local and explicitly cannot infer or mutate those external states. These remain future operational evidence under separate authority.
- Suite: regeneration/build commands. Why not required: source/generated artifacts are outside the candidate and regeneration would violate the zero-change boundary.

## CPU-heavy validation

`NOT REQUIRED`. No heavy suite could add proportionate evidence for a documentation-only candidate whose protected production bytes exactly match the baseline.

## Self-QA objective evidence

- Deterministic case: baseline-to-candidate byte/path boundary plus exact source-to-report spot traces.
- Verification layer: Git, static source inspection, schema/set comparison, isolated Node VM probes, and read-only generated projection.
- Browser justification, if any: none; browser execution would not verify any changed candidate behavior.
- Interaction checked: none changed. Future interaction contracts are correctly proposed rather than claimed as executed.
- Objective result: the corrected packet distinguishes whole-file from removable-block dispositions, preserves CSS without unproven selector deletion, consistently archives the Edge runtime outside the deployable path while removing its original path, and accurately classifies the current state carrier and reset/storage dependencies.

## Manual findings converted to invariants

- Finding: the first candidate had ambiguous whole-file/block dispositions and conflicting CSS/Edge recommendations. Defect class: governance record contradiction. Regression invariant: the production-path manifest controls whole-file dispositions; area reports label mixed files `MIGRATE/EXTRACT`, removable blocks separately, CSS `KEEP`, and the Edge runtime consistently `ARCHIVE` outside the deployable path with original-path removal. Corrected candidate: PASS.
- Finding: the first candidate's Archscry summary misclassified the current session carrier and used stale state/helper ranges. Defect class: source-trace inaccuracy. Regression invariant: exact line ranges and current carrier purpose must agree with the per-field source trace, and Maze's direct global read must not be described as a state-module import. Corrected candidate: PASS.
- Finding: the first candidate failed exact baseline `git diff --check`. Defect class: QA-0 formatting failure. Regression invariant: use baseline-to-candidate `git diff --check`, not only a clean-worktree default diff. Corrected candidate: PASS.

## Remaining Owner judgment

- ACCEPT or REJECT the proposed future purge scope and ordered prerequisites; this PASS does not authorize implementation or integration.
- Decide the product policy for conflicting legacy/profile/pending readings and the personal-reading portion of the Maze handoff when Forget occurs, while preserving unrelated Reading Finds and browsing context.
- Approve the future neutral location for generated faction context and the archive destination for authored Edge/SQL/deployment evidence under the applicable source/workflow owners.
- Decide remote Supabase schema, OAuth, Edge deployment, credentials, and user-data handling only after separately authorized live inventory. Repository evidence establishes no remote state.

No visual or browser product judgment remains for this unchanged-production candidate.

## Owner review commands / routes

1. Read `docs/audits/retired-code-recon/README.md`, especially Main findings, Exact proposed removal order, Uncertainty, and Owner Review.
2. Check the controlling rows for `assets/js/shared/shared.js`, `archscry/index.html`, `assets/js/home/home.js`, the Edge Function, generated context, deck modules, and preserved radar/Chart/CSS in `runtime-manifest.md`.
3. ACCEPT only if the proposal preserves local reading/Maze/Home contracts and keeps remote teardown plus future migration/implementation under separate authority; otherwise REJECT VM-654 with the specific proposal correction.

## Decisions, risks, and follow-up

Decision: RobQAPass PASS for exact candidate `dacca18131ccbb7b8c807a0fb388df1d68598426`; Owner Review may begin after the evidence-only handoff/card/index binding is recorded and checked.

Risks and uncertainties: real browser values, SDK-managed keys, remote schemas/data/deployments, and future implementation behavior remain unobserved. They are disclosed proposal prerequisites, not defects in this QA-0 candidate and not waived by PASS.

Follow-up recommendation: coordinator records this exact-SHA PASS, regenerates/checks generated views, validates the candidate-stage evidence, and stops at Owner Review. Any material report or production change invalidates this decision and requires a new candidate review.

Next suggested agent: Codex coordinator for evidence-only delivery binding, then Product Owner.

Related records: `docs/kanban/in-progress/VM-654-retired-code-recon.md`, `docs/audits/retired-code-recon/README.md`, `docs/handoffs/2026-09-14-0000-codex-vm654-retired-code-recon.md`.

## Not touched

No production HTML/JS/CSS, tests, package/CI, source/generated data, remote service, browser storage, deployment state, previous task lifecycle record, Git index, branch integration, or purge implementation was changed or accepted by this review.
