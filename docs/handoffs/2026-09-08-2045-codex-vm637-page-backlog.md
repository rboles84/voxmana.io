# VM-637 — Seven page-pass backlog stories

Agent: Codex (main agent, RobDev and Kanban coordination)
Date: 2026-09-08
Task requested: Create detailed backlog cards for each page pass, checking unused story numbers before allocation.
Status: Backlog intake delivered; page implementation has not started.
Related: VM-637, VM-642, VM-643, VM-644, VM-645, VM-646, VM-647, VM-648

## Preflight and files reviewed

Used the focused VM-637 context packet, its complete parent card, original handoff and referenced retention plan. Its disclosure reported one included direct handoff, one referenced plan, no ambiguous historical record and unavailable live remote main. Local Git confirms `main` and HEAD `45e9d61d017ff1a2d8f3a98165dbd7e7cc076bf7`. Existing VM-637 source/map/handoff and derived-view WIP were preserved.

Read current workflow, task-context generated-view contract, Kanban Steward role, and the full governing RobDev authority through its skill. The role required a Kanban Steward subagent; it independently checked exact available IDs and overlap. Its [attributed inventory](2026-09-08-2045-kanban-steward-vm637-numbering.md) records current card, branch, worktree and reference evidence. VM-641 was the highest allocated number; VM-642 through VM-648 were unused before allocation. VM-629 remains narrower repetition work, not a replacement for the page plan.

Recent accepted constraints remain: VM-625 Atlas shares dossier rendering, VM-634/635 visibility/background choices remain, and VM-636 matrix retention/removal needs a separate Owner scope choice. The main risk is turning proposed hides and future Owner writing into assumed implementation approval.

## What changed and why

Created seven self-contained Backlog stories in the requested order: VM-642 Home, VM-643 Archscry, VM-644 Maze, VM-645 Apocrypha, VM-646 Strategium, VM-647 Privacy and VM-648 Terms. Each carries its original detailed page checklist, explicit pending Owner decisions, acceptance criteria, source starting points, risks, coordination, validation expectations, review path, pickup prompt and PENDING delivery fields.

Linked them from the parent and source plan. Archscry retains three rounds and a representative-page approach; Strategium retains its small subpage passes. Related guide work is assigned to the corresponding page family. Policy cards separate factual service reconciliation from substantive legal judgments. Each story can enter normal context/admission/RobDev/RobQA/Owner/delivery when picked up; no branch or artificial admission was created for unscheduled intake.

## RobDev transfer packet

- Changed repository behavior: the seven planned passes are now independently retrievable authoritative backlog cards; parent and plan navigate to them.
- Owning sources and producer: authored cards/handoffs own intake facts; the existing task index writer produces board and handoff views.
- Reused machinery: current card fields, PENDING delivery record, task context and index regeneration. No new template framework, test harness or reservation ledger.
- Protected behavior: every runtime, policy text, source/data record, model score, relationship, saved state and accepted implementation remains unchanged. Existing planning WIP remains uncommitted.
- Risks/states: duplicate IDs, missing detail, incorrect references, contradictory sibling scope and premature approval; handled through preallocation discovery, explicit decision boundaries and source/document checks.
- Stop condition: intake only, no child implementation, new branch/worktree, commit, push, merge or publication.

## Files changed

Uncommitted intake inventory, not a candidate-bound implementation report: seven new child cards; updates to the existing VM-637 parent and retention map; this handoff and the Kanban Steward handoff; regenerated board and handoff index. The earlier mapping handoff remains untouched by this turn. Git status is the authority for the complete accumulated working copy, which includes prior-turn WIP. No material candidate, evidence head, integration or exact-SHA PASS is claimed.

## Validation

Applied RobQA for QA-0 document check selection; its full authority already read in this task is unchanged from the prior inspected revision (Git diff confirmed). No candidate-bound QA or independent engineering PASS is claimed for local backlog intake.

Initial source validation passed: exactly one authored card per allocated ID, all seven Status fields are Backlog, required fields/sections are present, each detailed checklist is included, and all 93 relative links in the child cards resolve. Likely source-file paths were checked before creation. Final validation includes generated-view write/check, inspection of both derived diffs, document whitespace/reference checks and confirmation that no runtime paths changed. Application/browser/engine suites are not applicable; CPU-heavy checks are not required.

Final results: all 143 relative links across the updated/new intake source documents resolve, whitespace checks and `git diff --check` pass, both generated views are fresh and their diffs contain the expected intake records plus preserved prior VM-637 WIP. A VM-643 context lookup resolves the new card. The first broad link probe misread an inline-code regex as a Markdown link; excluding code spans corrected that probe without changing content. The first index write could not write its Git-metadata recovery journal under the sandbox; the approved elevated invocation completed successfully. No runtime files changed.

## Decisions and uncertainties

Owner authorized card creation, not unconditional application of every recommendation. Page-specific product decisions and prose remain pending. No remote branch reservation claim is made; numbering evidence covers local authored sources, branches and the registered worktree. Live remote main was unavailable to the read-only context tool and is not needed to schedule unscheduled backlog work; it must be reconciled by admission before implementation.

## Not touched

Runtime, public copy, Privacy/Terms content, data, semantic authorities, generated production catalogs, imagery, storage, service configuration, existing task history and external communities.

## Follow-up and next suggested agent

Owner can pick VM-642 first or choose another page. The implementation agent should run that child's task context and admission, record the concrete scope/Owner copy decisions and continue the canonical process. Work on one page at a time; no seven-worktree initiative is implied. VM-637 remains the umbrella and VM-629 stays separate.

## Follow-up — Owner voice and evidence direction

The Owner explicitly directed all seven cards to preserve the overall goal: one dismissive response is not evidence for changing Vox Mana's voice. Added the same controlling goal near the top of every child, the parent, and the source plan. Separate accuracy, Owner intent, and observed task usability. Retaining existing wording is a successful disposition, and interpretations/opinions are not automatically removal targets. Broad changes call for feedback from several players doing real tasks; concrete factual corrections do not wait on that broad-change step. No outreach or invented feedback is authorized.

Updated summaries, pickup inventory, action-label framing, acceptance criteria and implementation prompts so the direction is operative rather than a footer disclaimer. Marked the earlier strict-release plan as optional historical proposal. Kept all stories Backlog and preserved runtime and prior implementation decisions. The Kanban Steward reviewed contradictory mandatory rewrite language through its existing attributed handoff.

Focused validation confirms all seven cards contain the three questions, preserve-voice rule, unchanged-retention outcome and operative prompt; all 93 child-card relative references resolve and card whitespace checks pass. Governing RobDev and unchanged RobQA document-check selection are reused. This remains uncommitted intake work at the same HEAD; no runtime changes, candidate or Owner acceptance is claimed. Generated-view freshness is checked after both agents finish their source notes.
